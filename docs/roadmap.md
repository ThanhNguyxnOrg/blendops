# Roadmap (Snapshot)

## Completed (MVP foundation)

- Repo scaffold with apps/packages split
- Shared schema + bridge core client
- Blender bridge with `scene.inspect`
- Vertical slice: `object.create` (CLI + MCP + bridge)
- Manual testing and eval prompt docs

## Next

1. `object.transform`
2. `scene.clear --confirm`
3. validation preset (`game-asset`)
4. export readiness checks
5. dry-run design and undo model hardening

## Non-goals for immediate next step

- arbitrary Python execution support
- large command-surface expansion without safety review
