# runtime-path-picker

Purpose: pick Path 1, Path 2, or CLI appendix; never treat Connector as standalone from Lab add-on.

Use when execution stack unclear.

Hand off to setup or CLI outline skills.
