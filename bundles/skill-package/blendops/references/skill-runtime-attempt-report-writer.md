# runtime-attempt-report-writer

Purpose: structured runtime attempt report with truth labels for Path 1, Path 2, or CLI appendix tries.

Use after connect attempts for provenance or stakeholder clarity.

Does not upgrade claims to Verified without artifacts.

Return Markdown sections A–F record.
