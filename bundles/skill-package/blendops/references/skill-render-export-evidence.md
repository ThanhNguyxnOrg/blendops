# render-export-evidence

Purpose: prove, verify, or downgrade preview/render/GLB claims.

Use whenever artifact status must be stated or checked.

Do not use to invent artifact claims from plans or transcripts.

Output contract: artifact status (`Not Produced`, `Produced`, `Verified`, or `Failed`), evidence path/visible proof, validation notes, and caveats.
