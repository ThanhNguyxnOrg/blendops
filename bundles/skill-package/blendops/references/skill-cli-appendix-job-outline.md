# cli-appendix-job-outline

Purpose: plan headless `blender --background` style jobs when MCP chat control is wrong; cite upstream CLI manual.

Use for CI/farm batch exports or policy-blocked MCP.

Do not ship opaque Python as the final user product surface.

Return phased outline + evidence hooks.
