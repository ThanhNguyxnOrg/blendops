# Decision tree

```txt
Need interactive MCP steering inside Blender?
  No → prefer CLI appendix planner for batch/export/render automation
  Yes → continue

Blender 5.1+ AND user will install Lab MCP add-on?
  Yes → Path 1 (pick host outline next)
  No → Path 2 IF user accepts community bridge profile

Multiple bridges configured on same instance?
  → STOP — runtime-bridge-conflict-resolver

Uncertain Blender version?
  → WARN branch: conditional recommendation table
```

Always restate: Connector is Path 1 **host option**, not a substitute for installing the Lab add-on inside Blender.
