# Triage next steps

| Symptom | Likely next action |
|---|---|
| Timeout on first tool call | Network/firewall, MCP transport mismatch, server not actually listening |
| Add-on not loading | Blender version vs Path 1 floor; wrong add-on zip; enable checkbox |
| Port already bound | Single-bridge / stray process — `runtime-bridge-conflict-resolver` |
| CLI silent failure | Wrong working directory; flag order; missing output path |
| Policy forbids MCP | Pivot outline to CLI appendix planner |

Always separate **BlendOps documentation responsibilities** from **upstream executable behavior**.
